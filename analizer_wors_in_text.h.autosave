#ifndef ANALIZER_WORS_IN_TEXT_H
#define ANALIZER_WORS_IN_TEXT_H

#include <map>
#include <iostream>
#include <iomanip>
#include <string>
#include <fstream>

t
using namespace std;

enum mod {path, console};

class analizer_wors_in_text
{
    map<string, unsigned> list_of_words;
    string path_input;
    string path_output;
    
public:
    analizer_wors_in_text(string, mod = path);
    analizer_wors_in_text();
};

#endif // ANALIZER_WORS_IN_TEXT_H
